> 🌐 本文档由 [earendil-works/pi](https://github.com/earendil-works/pi) 翻译,英文原版见原项目。

# 开发规则

## 对话风格

- 回答保持简短精炼
- commit、issue、PR 评论、代码中不使用 emoji
- 不写废话和客套填充文本(例如写 "Thanks @user",而不是 "Thanks so much @user!")
- 只用技术性文字,直接了当
- 使用简洁、清晰、朴素的语言;不得不用的行话先定义再使用
- 解释非平凡的设计和问题时,按"问题 → 具体示例或简短 trace → 解决方案"的顺序展开;说明为什么该方案是必要的,并与可选的复杂度区分开
- 优先给出具体行为和小型示例,而不是抽象总结、密集术语或未经解释的变更清单
- 当用户提问时,先回答问题,再做修改或执行实现类命令
- 回应用户反馈或某份分析时,先明确说明你同意还是不同意,再说明你改了什么

## 代码质量

- 在做大范围改动之前、编辑未完整查看过的文件之前、以及被要求调查或审计时,先完整读取文件。不要依赖搜索片段来做大改动。
- 除非绝对必要,不用 `any`。
- 只有一个调用点的单行辅助函数直接内联。
- 外部 API 类型去 node_modules 里查,不要猜。
- **禁止内联 import**(`await import()`、`import("pkg").Type`、动态类型 import)。只允许顶层 import。
- 绝不通过删除或降级代码来修复过时依赖导致的类型错误;应该升级依赖。
- 在根配置检查的代码中(`packages/*/src`、`packages/*/test`、`packages/coding-agent/examples`)只使用可擦除的 TypeScript 语法(Node strip-only 模式):不用参数属性、`enum`、`namespace`/`module`、`import =`、`export =` 以及其他需要 JS 产物的构造。使用带构造函数赋值的显式字段。
- 移除看起来是有意为之的功能或代码之前,必须先询问。
- 除非用户要求,不要保持向后兼容。
- 绝不硬编码按键判断(如 `matchesKey(keyData, "ctrl+x")`)。把默认值加进 `DEFAULT_EDITOR_KEYBINDINGS` 或 `DEFAULT_APP_KEYBINDINGS`,保持可配置。
- 绝不直接修改 `packages/ai/src/models.generated.ts`;应更新 `packages/ai/scripts/generate-models.ts` 后重新生成。在提交中附带 resulting `models.generated.ts` diff 总是可以的,即使重新生成带来了无关的上游模型元数据变更。

## 命令

- 代码改动后(文档除外):运行 `npm run check`(完整输出,不要 tail)。提交前修完所有 error、warning 和 info。它不跑测试。
- 未经用户要求,绝不运行 `npm run build` 或 `npm test`。
- 绝不直接跑完整 vitest 套件:其中包含在存在 endpoint/认证环境变量时才会激活的 e2e 测试。所有非 e2e 测试在仓库根目录运行 `./test.sh`。否则在包根目录运行指定测试:
  - Vitest:`node "$(git rev-parse --show-toplevel)/node_modules/vitest/dist/cli.js" --run test/specific.test.ts`
  - `packages/tui`(`node:test`):`node --test test/specific.test.ts`
- 如果你创建或修改了测试文件,要运行它并迭代测试或实现,直到通过。
- `packages/coding-agent/test/suite/` 使用 `test/suite/harness.ts` + faux provider。不使用真实 provider API、密钥或付费 token。
- 为修复 GitHub issue 而写回归测试时,在测试旁边加一条带 issue 编号的注释。
- 临时脚本写到临时文件(如 `/tmp`),运行、按需修改、用完删除。不要在 `bash` 命令里内嵌多行脚本。
- 未经用户要求,绝不 commit。

## 依赖与安装安全

- 把 npm 依赖和 lockfile 变更当作需评审的代码对待。直接外部依赖固定到精确版本。
- 更新 `undici` 时,必须先阅读目标版本的 changelog/release notes,评估是否有影响功能的变更,再执行更新。
- 本地安装/更新用 `npm install --ignore-scripts`;干净/CI 式安装用 `npm ci --ignore-scripts`。未经用户要求不运行生命周期脚本。
- 依赖元数据变化时,用 `npm install --package-lock-only --ignore-scripts` 刷新 `package-lock.json`。
- 如需重新生成 `packages/coding-agent/npm-shrinkwrap.json`,运行 `node scripts/generate-coding-agent-shrinkwrap.mjs`(用 `--check` 或 `npm run check` 验证)。带生命周期脚本的新依赖需要评审并在该脚本中加入显式 allowlist;绝不悄悄添加。
- pre-commit 会阻止 lockfile 提交,除非设置 `PI_ALLOW_LOCKFILE_CHANGE=1`。除非用户确实要提交 lockfile 变更,否则不要绕过。

## Git

多个 pi 会话可能同时在同一个 cwd 中运行,各自修改不同文件。触及未暂存、已暂存或未跟踪文件的 Git 操作,若超出你自己的改动范围,会破坏其他会话的工作。遵守以下规则:

提交:

- 只提交你在本次会话中改过的文件。
- 显式指定路径暂存(`git add <path1> <path2>`);绝不 `git add -A` / `git add .`。
- 提交前运行 `git status`,确认只暂存了你自己的文件。
- `packages/ai/src/models.generated.ts` 总是可以随你的文件一起提交。
- 消息格式:`{feat,fix,docs}[(ai,tui,agent,coding-agent)]: <commit message>`(可多行)。消息要有信息量且简洁。

绝不运行(会破坏其他 agent 的工作或绕过检查):

- `git reset --hard`、`git checkout .`、`git clean -fd`、`git stash`、`git add -A`、`git add .`、`git commit --no-verify`。

如果 rebase 出现冲突:

- 只解决你自己修改过的文件中的冲突。
- 冲突出现在你没有修改的文件中时,中止并询问用户。
- 绝不 force push。

## Issue 与 PR

贡献者门槛(自动关闭工作流、`lgtm`/`lgtmi`、质量标准)见 `CONTRIBUTING.md`。

审阅 PR 时:

- 除非用户明确要求,不要运行 `gh pr checkout`、`git switch` 或其他把 worktree 切到 PR 分支的操作。
- 用 `gh pr view`、`gh pr diff`、`gh api` 以及对已拉取 ref 的本地 `git show`/`git diff` 来查看 PR 元数据、commit 和补丁,不切换分支。
- 需要 PR 文件内容时,拉取/读取到临时文件,或用 `git show <ref>:<path>`,不切换分支。

创建 issue 时:

- 为受影响的包加 `pkg:*` 标签(`pkg:agent`、`pkg:ai`、`pkg:coding-agent`、`pkg:tui`);适用几个加几个。

发布 issue/PR 评论时:

- 把评论写到临时文件,用 `gh issue/pr comment --body-file` 发布(绝不通过 `--body` 传多行 markdown)。
- 评论保持简洁、技术性,贴近用户的语气。
- 每条 AI 发布的评论都以发起 prompt 中规定的 AI 生成免责声明结尾(如 ``This comment is AI-generated by `/wr```)。

通过 commit 关闭 issue 时:

- 在提交消息中包含 `fixes #<number>` 或 `closes #<number>`,合并时即自动关闭 issue。多个 issue 时逐个重复关键字(`closes #1, closes #2`);共享写法(`closes #1, #2`)只会关闭第一个。

## 用 tmux 测试 pi 交互模式

在受控终端中运行 TUI(仓库根目录下):

```bash
tmux new-session -d -s pi-test -x 80 -y 24
tmux send-keys -t pi-test "./pi-test.sh" Enter
sleep 3 && tmux capture-pane -t pi-test -p     # capture after startup
tmux send-keys -t pi-test "your prompt here" Enter
tmux send-keys -t pi-test Escape               # special keys (also C-o for ctrl+o, etc.)
tmux kill-session -t pi-test
```

## 变更日志

位置:`packages/*/CHANGELOG.md`(每个包一份)。

`## [Unreleased]` 下的章节:`### Breaking Changes`(需要迁移的 API 变更)、`### Added`、`### Changed`、`### Fixed`、`### Removed`。

规则:

- 所有新条目写在 `## [Unreleased]` 下。先读完整个章节,再追加到现有子章节;绝不重复创建子章节。
- 已发布版本章节(如 `## [0.12.2]`)不可变;绝不修改。
- 在 `main` 以外的分支或 PR 上工作时,不创建 changelog 条目。

署名:

- 内部(来自 issue):`Fixed foo bar ([#123](https://github.com/earendil-works/pi/issues/123))`
- 外部贡献:`Added feature X ([#456](https://github.com/earendil-works/pi/pull/456) by [@username](https://github.com/username))`

## 发布

**Lockstep 版本号**:所有包共享一个版本号;每次发布同时更新全部。`patch` = 修复 + 新增,`minor` = 破坏性变更。没有 major 发布。

1. **更新 CHANGELOG**:询问用户是否已在 `main` 最新 commit 上运行过 `/cl` prompt。如果没有,必须先运行 `/cl` 审计并更新每个包的 `[Unreleased]` 章节,然后再发布。

2. **本地冒烟测试**:构建一个未发布的 release,并在仓库外做冒烟测试(避免解析到 workspace 文件):
   ```bash
   npm run release:local -- --out /tmp/pi-local-release --force
   cd /tmp

   # Node package install smoke tests
   /tmp/pi-local-release/node/pi --help
   /tmp/pi-local-release/node/pi --version
   /tmp/pi-local-release/node/pi --list-models
   /tmp/pi-local-release/node/pi -p "Say exactly: ok"
   /tmp/pi-local-release/node/pi

   # Bun binary smoke tests
   /tmp/pi-local-release/bun/pi --help
   /tmp/pi-local-release/bun/pi --version
   /tmp/pi-local-release/bun/pi --list-models
   /tmp/pi-local-release/bun/pi -p "Say exactly: ok"
   /tmp/pi-local-release/bun/pi
   ```
   验证 Node 和 Bun 的启动、模型/账户列表、交互模式启动,并用预期的默认 provider 跑至少一次真实 prompt。裸命令 `/tmp/pi-local-release/node/pi` 和 `/tmp/pi-local-release/bun/pi` 会启动交互模式;在 tmux 中分别运行、提交一个 prompt、等到模型回复,交互冒烟测试才算通过。任何失败都是发布阻塞项,除非用户明确接受风险。

3. **运行发布脚本**:
   ```bash
   PI_ALLOW_LOCKFILE_CHANGE=1 npm_config_min_release_age=0 npm run release:patch    # fixes + additions
   PI_ALLOW_LOCKFILE_CHANGE=1 npm_config_min_release_age=0 npm run release:minor    # breaking changes
   ```
   `npm_config_min_release_age=0` 只在发布命令上使用。否则仓库正常的 npm 年龄门槛可能在当前 workspace 包版本刚发布不久时阻塞发布期的 lockfile 刷新。push 前审阅发布脚本产生的所有 lockfile/shrinkwrap diff。

   发布脚本会 bump 所有包版本、更新 changelog、重新生成发布产物、运行 `npm run check`、提交 `Release vX.Y.Z`、打 tag `vX.Y.Z`、添加新的 `## [Unreleased]` changelog 章节、提交 `Add [Unreleased] section for next cycle`,然后 push `main` 和 tag。tag 已 push 后不要重跑发布脚本。

4. **CI 验证并公告 npm 发布**:push `vX.Y.Z` tag 会触发 `.github/workflows/build-binaries.yml`。`publish-npm` job 通过 GitHub Actions OIDC(enviroment `npm-publish`)使用 npm trusted publishing;无需本地 `npm publish`、`npm whoami`、OTP 或 WebAuthn 流程。发布后,`announce-pi-dev-release` 会验证每个公开 workspace 包都能解析到确切的发布版本且其 npm tarball 可用,然后把已验证的 release 标记写入 R2。`pi.dev/api/latest-version` 读取该标记;在此 job 成功前绝不能公告来自 npm 的新 release。

5. **如果 CI 发布或公告失败**:检查失败的 job。发布辅助逻辑是幂等的,会跳过 npm 上已存在的包版本;公告 job 在更新 R2 标记前会重新检查可用性。修复 CI 或临时性 npm 问题后,重跑失败的 job 或 workflow。不要对同一版本重跑 `npm run release:patch` 或 `npm run release:minor`。

## 用户覆盖

如果用户的指令与本文档任何规则冲突,先请求显式确认,再执行其指令。
